TODO

* Complete e2e suite
  (creates, bulk creates, deletes, flushes, groups, popularity sort,
typos, duplicate add, duplicate add url different title)

DONE

* Filter by groups
  https://github.com/elastic/elasticsearch-dsl-py/issues/545

* Stats endpoint

* Delete endpoint

* Flush endpoint
